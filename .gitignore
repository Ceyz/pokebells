# Game ROM and IP-derived assets — NEVER commit
pokemon_red.gb
*.gb
*.gbc
*.sav
shin_patch.zip
*.bps
*.ips

# ROM chunks generated for inscriptions (encoded ROM)
phase1/chunks/

# Reverse-engineering artifacts (Nintondo extension/viewer bundles)
phase1/tmp-*

# Local TLS dev certs (private keys!)
phase1/.https-cert/
*.pem
*.key

# Local Claude config
.claude/

# Internal planning / private working notes
memory.md
pokebells_roadmap*.html

# Local research / prototype work not meant for the public companion repo
phase1/*
poc/
download.py
patch-rom.py
poc-memory.html
test-nintondo.html

# Phase 4.5 relay — transparent transport layer, safe to publish
# (no ROM, no IP, pure WebSocket relay + signature verification)
!phase1/partykit-probe/
phase1/partykit-probe/node_modules/
phase1/partykit-probe/.partykit/
!phase1/signin-verify.mjs
!phase1/pbrp/
!.github/workflows/partykit-cloudflare.yml

# Dev/build artifacts
node_modules/
.partykit/
dist/
build/
*.log
.DS_Store
Thumbs.db
desktop.ini

# Editor noise
.vscode/
.idea/
*.swp
*.swo
*~

# Local environment overrides
.env
.env.local
