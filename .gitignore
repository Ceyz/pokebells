# Game ROM and IP-derived assets — NEVER commit
pokemon_red.gb
*.gb
*.gbc
*.sav
shin_patch.zip
*.bps
*.ips

# ROM chunks generated for inscriptions (encoded ROM)
game/chunks/

# Reverse-engineering artifacts (Nintondo extension/viewer bundles)
game/tmp-*

# Local TLS dev certs (private keys!)
game/.https-cert/
*.pem
*.key

# Local Claude config
.claude/

# Internal planning / private working notes
memory.md
pokebells_roadmap*.html

# Local research / prototype work not meant for the public companion repo
game/*
poc/
download.py
patch-rom.py
poc-memory.html
test-nintondo.html

# One-shot dev tools (sprite pack generator, future extraction scripts).
# Private by default; allowlist specific files here if/when ready to publish.
tools/

# Phase 4.5 relay — transparent transport layer, safe to publish
# (no ROM, no IP, pure WebSocket relay + signature verification)
!game/partykit-probe/
game/partykit-probe/node_modules/
game/partykit-probe/.partykit/
!game/cf-relay/
game/cf-relay/node_modules/
game/cf-relay/.wrangler/
!game/indexer/
game/indexer/node_modules/
game/indexer/.wrangler/
!game/signin-verify.mjs
!game/pbrp/
!.github/workflows/cf-relay.yml
!.github/workflows/indexer.yml

# Synthesized by pages.yml at deploy — source of truth is game/pbrp/
companion/pokebells/pbrp/
companion/pokebells/signin-verify.mjs

# Dev/build artifacts
node_modules/
.partykit/
dist/
build/
*.log
.DS_Store
Thumbs.db
desktop.ini

# Editor noise
.vscode/
.idea/
*.swp
*.swo
*~

# Local environment overrides
.env
.env.local
