name = "pokebells-relay"
main = "src/worker.js"
compatibility_date = "2026-04-19"
workers_dev = true

[[durable_objects.bindings]]
name = "RELAY_ROOM"
class_name = "RelayRoom"

[[migrations]]
tag = "v1"
new_classes = ["RelayRoom"]

# Uncomment after first deploy succeeds to bind the custom domain.
# Requires the CLOUDFLARE_API_TOKEN to have Workers Scripts:Edit,
# Zone:Zone:Edit, Zone:DNS:Edit on the bellforge.app zone.
#
# [[routes]]
# pattern = "party.bellforge.app"
# custom_domain = true
